server:
  port: 8282

spring:
  application:
    name: SPRING-SECU
  datasource:
    url: jdbc:mysql://localhost:3306/spring_secu?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true